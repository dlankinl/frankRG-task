version: '3.8'

services:
  postgres-db:
    image: postgres:latest
    container_name: postgres-fs-db
    env_file:
      - pg.env
    ports:
      - '5432:5432'
    volumes:
      - postgres-db:/var/lib/postgresql/data

volumes:
  postgres-db:
    driver: local